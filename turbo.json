{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build:pkg": {
      "dependsOn": ["^build:pkg"],
      "outputs": ["dist/**", "dist-type-bundles/**"],
      "outputMode": "errors-only"
    },
    "build:app": {
      "dependsOn": ["^build:app", "build:pkg"],
      "outputs": [".next/**", "!.next/cache/**", "storybook-static/**", "out/**"],
      "outputMode": "new-only"
    },
    "types": {},
    "test": {},
    "lint": {},
    "publint": {},
    "dev:pkg": {
      "cache": false,
      "persistent": true
    },
    "dev:app": {
      "cache": false,
      "persistent": true
    }
  }
}
