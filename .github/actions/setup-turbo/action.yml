name: Setup turbo
description: ""

runs:
  using: "composite"

  steps:
    - name: Setup turbo cache
      uses: actions/cache@v4
      with:
        path: .turbo
        key: ${{ runner.os }}-turbo-${{ github.sha }}
        restore-keys: |
          ${{ runner.os }}-turbo-
